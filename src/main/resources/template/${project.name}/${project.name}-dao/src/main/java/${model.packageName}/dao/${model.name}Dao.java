package ${model.packageName}.dao;

import ${model.packageName}.model.${model.name};
import com.baomidou.mybatisplus.mapper.BaseMapper;

public interface ${model.name}Dao extends BaseMapper<${model.name}> {

}
